package pageobjects.youtube;

import org.openqa.selenium.WebDriver;
import pageobjects.pagecontracts.PageCommon;

abstract public class InnerInfoPane extends PageCommon {

    protected InnerInfoPane(WebDriver driver) {
        super(driver);
    }

}
